# This file is used by `go.sh` to install the same version of ICEkit as
# specified in this project's `Dockerfile`.
#
# IMPORTANT! If you change the pinned version in one place, you must also change
# it in the other.

-e ..[api,brightcove,dev,django18,docs,forms,glamkit,project,search,test]
